# CORS Settings
# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Database Settings
DATABASE_URL=postgresql://user:password@localhost/react_fastapi_db

# Other API settings (optional)
# PORT=8000
# HOST=0.0.0.0